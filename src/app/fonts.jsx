import { Inter, Inder, Montserrat, Raleway, Poppins } from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
})

export const inder = Inder({
    subsets: ['latin'],
    display: 'swap',
    weight: "400"
})

export const montserrat = Montserrat({
    subsets: ['latin'],
    display: 'swap'
})

export const raleway = Raleway({
    subsets: ['latin'],
    display: 'swap'
})

export const poppinsRegular = Poppins({
    subsets: ['latin'],
    display: 'swap',
    weight: "400"
})

export const poppinsBold = Poppins({
    subsets: ['latin'],
    display: 'swap',
    weight: "700"
})